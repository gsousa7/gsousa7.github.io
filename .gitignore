# Generated by scripts/rendercv_convert.py at build time (see
# .github/workflows/pages.yml) — run it locally to preview, but the
# committed repo never needs to carry the binary.
assets/files/Goncalo_Sousa_CV.pdf
